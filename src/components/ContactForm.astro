---
---
<section class="bg-gray-900 text-white">
  <div class="mx-auto max-w-screen-xl px-4 py-16 sm:px-6 lg:px-8">
    <div class="grid grid-cols-1 gap-x-16 gap-y-8 lg:grid-cols-5">
      <div class="lg:col-span-2 lg:py-12">
        <p class="max-w-xl text-lg">
         <span class="text-blue-500">Get In Touch</span><br>
         <strong class="text-3xl font-bold sm:text-4xl">Your Projects, Your Questions, Our Answers</strong>
        </p>
        <p class="mt-4">
            Feel free to reach out for inquiries, feedback, or collaborations. We're here to hear from you and help in any way we can.
        </p>
      </div>

      <div class="rounded-lg bg-gray-800 p-8 shadow-lg lg:col-span-3 lg:p-12">
        <form action="" class="space-y-4">
          <div>
            <label class="sr-only" for="name">Name</label>
            <input
              class="w-full rounded-lg border-gray-700 bg-gray-900 p-3 text-sm"
              placeholder="Your name*"
              type="text"
              id="name"
            />
          </div>

          <div class="grid grid-cols-1 gap-4 sm:grid-cols-2">
            <div>
              <label class="sr-only" for="email">Email</label>
              <input
                class="w-full rounded-lg border-gray-700 bg-gray-900 p-3 text-sm"
                placeholder="Email*"
                type="email"
                id="email"
              />
            </div>

            <div>
              <label class="sr-only" for="phone">Phone</label>
              <input
                class="w-full rounded-lg border-gray-700 bg-gray-900 p-3 text-sm"
                placeholder="Your Phone*"
                type="tel"
                id="phone"
              />
            </div>
          </div>

           <div class="grid grid-cols-1 gap-4 sm:grid-cols-2">
            <div>
              <label class="sr-only" for="country">Country</label>
              <input
                class="w-full rounded-lg border-gray-700 bg-gray-900 p-3 text-sm"
                placeholder="Country*"
                type="text"
                id="country"
              />
            </div>

            <div>
            </div>
          </div>

          <div>
            <label class="sr-only" for="message">Message</label>

            <textarea
              class="w-full rounded-lg border-gray-700 bg-gray-900 p-3 text-sm"
              placeholder="Message"
              rows="8"
              id="message"
            ></textarea>
          </div>

          <div class="mt-4">
            <button
              type="submit"
              class="inline-block w-full rounded-full bg-gradient-to-r from-blue-500 to-cyan-400 px-5 py-3 font-medium text-white sm:w-auto"
            >
              Send Message
            </button>
          </div>
        </form>
      </div>
    </div>
  </div>
</section>
